  <main class="max-w-4xl mx-auto py-10 px-4">
    <section class="bg-white rounded-lg shadow p-6 mb-8">
      <h2 class="text-2xl font-semibold text-blue-600 mb-4">O que é HTML?</h2>
      <p class="mb-4"><strong>HTML (HyperText Markup Language)</strong> é a linguagem responsável pela estrutura de uma página web. Com ela, você define títulos, parágrafos, imagens, links, listas e muito mais.</p>
      <p class="mb-4">Exemplo básico de estrutura HTML:</p>
      <pre class="bg-gray-100 p-4 rounded text-sm overflow-auto"><code>&lt;!DOCTYPE html&gt;
&lt;html&gt;
  &lt;head&gt;
    &lt;title&gt;Minha Página&lt;/title&gt;
  &lt;/head&gt;
  &lt;body&gt;
    &lt;h1&gt;Olá Mundo!&lt;/h1&gt;
  &lt;/body&gt;
&lt;/html&gt;
</code></pre>
    </section>

    <section class="bg-white rounded-lg shadow p-6 mb-8">
      <h2 class="text-2xl font-semibold text-blue-600 mb-4">O que é CSS?</h2>
      <p class="mb-4"><strong>CSS (Cascading Style Sheets)</strong> é a linguagem usada para estilizar o conteúdo HTML. Com ele, você pode definir cores, tamanhos, espaçamentos, posicionamentos e muito mais.</p>
      <p class="mb-4">Exemplo básico de CSS:</p>
      <pre class="bg-gray-100 p-4 rounded text-sm overflow-auto"><code>h1 {
  color: blue;
  font-size: 24px;
  text-align: center;
}</code></pre>
    </section>

    <section class="bg-white rounded-lg shadow p-6 mb-8">
      <h2 class="text-2xl font-semibold text-blue-600 mb-4">Caso de uso real</h2>
      <p class="mb-4">Suponha que você deseje criar uma landing page simples para divulgar um produto. Usando HTML e CSS, você pode:</p>
      <ul class="list-disc list-inside space-y-2 mb-4">
        <li>Estruturar os conteúdos como títulos, textos, imagens e botões (HTML)</li>
        <li>Definir um layout atraente, cores da marca e fontes personalizadas (CSS)</li>
        <li>Adaptar o site para celulares e tablets com responsividade (CSS)</li>
      </ul>
      <p>Com apenas HTML e CSS, já é possível criar sites profissionais e visualmente bonitos.</p>
    </section>
  </main>